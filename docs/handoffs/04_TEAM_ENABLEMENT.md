# HANDOFF Lane 4: team enablement

Paste into a fresh OB-Nucleus project chat. You are Chief, lane 4 of 6. Read first: handoffs/00_INDEX_THE_LARGER_PICTURE.md, docs/ob-nucleus-operations.sop.md (the curriculum), repo STATUS.md.

## Mission

The humans of OB.1 (Chloe with Claudia, Andrew, Evan, future hires) operate Audity, Nucleus, and BlueprintOS fluently through plain language, without touching the Audity web app for routine work, and without ever mishandling a token. The SOP is written; your job is adoption, feedback, and iteration. Technology that only Chief can drive is not yet institutional capability.

## What exists and is verified

- docs/ob-nucleus-operations.sop.md v1.0.0: setup paths (technical and zero-setup), daily sweep reading, client-work queries, Nucleus memory conventions, gated-write requests, error handling, success criteria checklist.
- The daily digest lands in the Drive folder at 07:00 (read_sweep_<date>.md).
- TOKEN_REGISTRY.md (Drive folder): redacted registry, one PAT per surface, quarterly rotation dates. Andrew's existing token is read-only by design.
- config/nucleus_conventions.md: the memory writing rules the SOP summarizes (addressable subjects, one fact per memory, projectId discipline, promote not duplicate).

## Scope (yours)

1. Run each person through the SOP success criteria live: pull the sweep, name the top unconverted leads, query an engagement, capture a note, watch a dry run. Score the checklist per person; log results in STATUS.md.
2. Issue personal PATs where warranted (each human creates their own in their browser; you record label, scope, fingerprint, rotation date in TOKEN_REGISTRY.md). Read scope is the default; write scope is a Chris decision.
3. Collect friction: every place a human stumbles is an SOP defect. Ship v1.1 with fixes; keep the doc honest against what the tools actually do.
4. Coordinate with lane 2 for any seat that wants the MCP tools directly; the SOP's zero-setup path (ask Chief/Claudia/Buddy) covers everyone else today.

## Non-goals (other lanes)

Tool building (lanes 1-3). Connector installs (lane 2 executes; you schedule). Client-facing materials (lane 5). Do not train on workflows that are still dry-run-only; teach what is default-on.

## First three actions

1. Confirm today's digest exists and reads clean; that is the first artifact every trainee touches.
2. Book 30 minutes each with Chloe and Andrew; send them the SOP link and the three questions they should arrive able to ask.
3. Branch chief/lane-04-enablement for SOP revisions.

## Definition of done

Three humans pass the success criteria unaided; TOKEN_REGISTRY.md current with rotation dates; SOP v1.1 shipped from real feedback; STATUS.md lane update written.

## Needs humans

Chloe, Andrew, Evan: 30 minutes each. Chris: write-scope decisions only. No credits involved anywhere in this lane.
